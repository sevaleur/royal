

nav.navigation
  .navigation__wrapper
    .navigation__logo
      a.navigation__logo__link(href='/')=meta.data.title
        img.navigation__logo__link__image(src=meta.data.image.url)
    ul.navigation__list
      each item in navigation.data.links
        li.navigation__list__item
          a.navigation__list__item__link(Href=Link(item.link))=item.link_name
